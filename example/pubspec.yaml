name: example
description: Demonstrates world_csc_picker
publish_to: none

# ← Add this:
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  world_csc_picker:
    path: ../

flutter:
  uses-material-design: true
  assets:
    - assets/worldcities.json
